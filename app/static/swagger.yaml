# Yaml documentation API initializating

swagger: '2.0'
info:
  title: "Library API"
  description: "API to manage library system"
  version: "1.0.0"
host: "127.0.0.1.500"
schemes: 
  - "https"
consumes:
  - "application/json"
produces:
  - "application/json"
paths:
definitions:

  LoginCredentials:
    type: "object"
    properties:
      email:
        type: "string"
      password:
        type: "string"

      required:
        - email
        - password

  LoginResponse:
    type: "object"
    properties:
      token:
        type: "string"
      message:
        type: "string"
      status:
        type: "string"

